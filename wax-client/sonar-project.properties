# SonarCloud Configuration
sonar.projectKey=JuanDiego-sc_wax-client
sonar.organization=juandiego-sc

sonar.projectName=WAX Client
sonar.projectVersion=0.0.0

# Source configuration
sonar.sources=src
sonar.exclusions=**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx,**/node_modules/**,**/dist/**

# Coverage exclusions — solo medimos componentes (.tsx).
# Los .ts (api, hooks, utils, types, services, lib, config, routes) no cuentan
# contra el % de cobertura. Sonar sigue analizándolos para code smells y bugs.
sonar.coverage.exclusions=src/**/*.ts,src/main.tsx,src/**/*.d.ts

# TypeScript / JavaScript
sonar.typescript.lcov.reportPaths=coverage/lcov.info
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Encoding
sonar.sourceEncoding=UTF-8
